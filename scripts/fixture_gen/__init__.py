"""Declarative generator for the synthetic fixture corpus.

`models/` is the schema, `corpus/` is the data, `sampling.py` and `writers.py` are the engine.
See `scripts/generate_fixtures.py` for the entry point.
"""  # noqa: E501
